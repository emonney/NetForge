# Our stack runs ahead of some packages' peer ranges (e.g. i18next 24's peer still pins
# typescript <6, but we're on TS 6). Use npm's legacy peer behaviour so a clean `npm install`
# — including the one MSBuild runs on `dotnet build` — resolves instead of ERESOLVE-failing.
legacy-peer-deps=true
