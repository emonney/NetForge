// Route metadata, read by the app shell (title, access control) in later phases.
// Plain module — generouted only registers .tsx/.jsx files, so this is never a route.
export const meta = {
  title: 'Template',
  permissions: [] as string[],
};
