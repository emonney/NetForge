/**
 * The scaffold's starting brand accent colour, chosen at creation time (the configurator picker or
 * `dotnet new netforge --brandColor "#4f46e5"`). The template engine replaces the placeholder below; an
 * un-scaffolded checkout keeps the literal, which isn't a valid colour and so falls back to the default.
 *
 * This is only the *initial* brand — an admin overrides it at runtime from /admin/appearance.
 */
export const DEFAULT_BRAND_COLOR = '';

/** The scaffold's starting theme key (e.g. "ocean"), set at creation time. Replaced by the template engine;
 * an un-scaffolded checkout keeps the literal, which isn't a known theme and so falls back to the default. */
export const DEFAULT_THEME = 'default';
